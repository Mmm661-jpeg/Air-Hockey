{
    "name":"air-hockey-root",
    "scripts": {
        "install-all":"cd server && npm install && cd ../client/airhockey-front && npm install",
        "build": "cd client/airhockey-front && npm run build",
        "start": "cd server && npm start"

    }
}